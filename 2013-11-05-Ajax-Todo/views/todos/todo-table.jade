table#todos
  thead
    tr
      th Title
      th Due Date
      th Priority
  tbody
    each todo in todos
      tr(style='background-color:#{todo.priority.color};')
        td= todo.title
        td= todo.dueDate
        td= todo.priority.name

